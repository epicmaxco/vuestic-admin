const config = (state: any) => state.app.config
const palette = (state: any) => state.app.config.palette
const isLoading = (state: any) => state.app.isLoading

export {
  config,
  palette,
  isLoading,
}
